# Copy to .env and adjust as needed. Used by docker-compose.deploy.yml.
# No secrets today -- if one's ever added, leave it blank here with a comment on
# where the value comes from (non-secret vars can keep a sensible inline default,
# like HOST_PORT below).
HOST_PORT=8501  # port the app listens on
