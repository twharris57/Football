# Copy to .env and adjust as needed. Used by docker-compose.deploy.yml.
# No secrets today -- if one's ever added here, mark it inline with "# SECRET: ..."
# so nas-configs' deployment-reference convention picks it up correctly.
HOST_PORT=8501  # non-secret: port the app listens on
